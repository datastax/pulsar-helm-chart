apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    url: "{{ PULSAR_PROMETHEUS_URL }}"
    access: proxy
    isDefault: true
    jsonData:
      timeInterval: 30s